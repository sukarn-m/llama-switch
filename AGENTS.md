# AGENTS.md

> **⚠ Project-specific configuration.** The runtime `config.yaml` (gitignored) contains machine-specific paths, GPU topology, and bind addresses. Do not assume any example values — always review `config.yaml` for your own deployment.

Guidance for AI coding agents working in the `llama-switch` codebase.

## What this project is

`llama-switch` is a **GPU model multiplexer for `llama-server`** (from llama.cpp). It is an HTTP reverse proxy that presents a single OpenAI-compatible endpoint and transparently loads/unloads different GGUF models on one or more GPUs behind the scenes. It was written to replace a shell script (`llama-server.sh`) that ran a single fixed model — now many models can coexist and be switched on demand within VRAM and concurrency limits.

Key idea: a client sends `POST /v1/chat/completions` with `{"model": "my-model", ...}`. The proxy parses the `model` field, ensures that model's `llama-server` backend is running (spawning it if needed, evicting least-recently-used models to make room), and forwards the request — including SSE streaming — to that backend on a dynamically-allocated loopback port.

It is a **single-binary Go program** with no subpackages and one third-party dependency (`gopkg.in/yaml.v3`). Target environment is NVIDIA CUDA multi-GPU Linux (it shells out to `nvidia-smi` for VRAM accounting).

## Repository layout

Everything lives at the repo root in `package main`. There are no subdirectories and no internal packages.

| File | Responsibility |
|------|----------------|
| `main.go` | CLI entry point (`serve`, `profile`, `models`, `status`, `help`), usage text, graceful shutdown, VRAM profiling loop. `profileSingle` returns `(int, []int, error)` — total VRAM and per-GPU slice. |
| `config.go` | YAML config types (`Config`, `ServerConfig`, `BackendConfig`, `ModelConfig`), loading, defaults, `~`/`$VAR` path expansion, llama-server argument & env building. |
| `backend.go` | `Backend` (one llama-server process) and `BackendManager` (lifecycle, port allocation, health checks, LRU eviction, idle sweeper). Core of the system. |
| `proxy.go` | `ProxyServer` — the client-facing HTTP server. Routing, model extraction from request body, streaming/non-streaming forwarding, hop-by-hop header stripping. |
| `vram.go` | `nvidia-smi` VRAM querying, `VRAMCache` JSON sidecar load/save, per-GPU VRAM estimation and admission control (`vramFitsPerGPU`, `vramEstimatePerGPU`, `computePerGPUDelta`, `deviceNameToIndex`, `modelFileSizeMB`, `validateVRAMMeasurement`). |
| `logger.go` | `CondLogger` — thin `Printf` wrapper over stdout. |
| `config.yaml` | **Runtime config (gitignored).** The actual models, paths, GPU topology, and server settings for this deployment. Edit carefully; it is the single source of truth. |
| `vram-cache.json` | Generated by `llama-switch profile`. Model ID → measured VRAM in MB, including per-GPU breakdown (`gpu_vram` field). Regeneratable; safe to delete. |
| `llama-switch.service` | systemd user unit for running `serve` as a service. |
| `go.mod` / `go.sum` | Module `llama-switch`, Go 1.26, single dependency. |
| `llama-switch` | Compiled binary (checked in). Rebuild with `go build`. |

## Architecture & request flow

```
Client ──HTTP──▶ ProxyServer (:8080)
                   │
                   ├─ GET  /                → health
                   ├─ GET  /v1/models       → OpenAI model list (with status field)
                   ├─ GET  /models          → llama.cpp router model list (with status)
                   ├─ POST /models/load     → explicitly load a model {"model":"id"}
                   ├─ POST /models/unload   → explicitly unload {"model":"id"} (Open WebUI "Eject")
                   ├─ GET  /v1/loaded       → legacy: list loaded models only
                   └─ everything else       → handleProxy
                                            │
                    parse "model" from JSON body
                                            ▼
                   BackendManager.EnsureLoaded(modelID)
                     ├─ already loaded? → reuse
                     ├─ else: ensureCapacityLocked
                     │    ├─ enforce MaxModels (evict LRU)
                     │    └─ enforce per-GPU VRAM + 1GB headroom on GPU0 (evict LRU)
                     ├─ startBackendLocked: alloc port, spawn llama-server,
                     │    wait for /health 200 (HealthTimeoutSeconds)
                     └─ return *Backend
                                            ▼
                   forward() → proxy to 127.0.0.1:<backend port>,
                   stream response back if "stream":true
```

- Each loaded model = one `llama-server` child process on its own port from `backend_port_start`–`backend_port_end`.
- Backends are addressed only on loopback (`127.0.0.1`); the public listener binds to the configured `host` (set in `config.yaml`).
- The idle sweeper goroutine (`idleSweeper`) unloads any backend untouched for `idle_timeout_minutes`, polled every `sweep_interval_seconds`.

## Configuration reference (`config.yaml`)

- `server`: `host`, `port`, `backend_port_start/end`, `max_models`, `idle_timeout_minutes`, `health_timeout_seconds`, `sweep_interval_seconds`.
- `backend`: `binary` (path to `llama-server`), `env` (notably `LD_LIBRARY_PATH` for CUDA libs), `common_args` (flags prepended to every model invocation), `workdir`, `nvidia_smi`.
- `models`: list of `ModelConfig`. Lookup by `id`, `alias`, or `model` name (`FindModel` matches all three). Each builds its llama-server args via `ModelConfig.BuildArgs`. Supports `mmproj` (multimodal), `tensor_split`, `ctx_checkpoints`, and freeform `extra_args`.
- Path expansion: `~` and `$VAR` are expanded in config values (`expand()`), including colon-separated `LD_LIBRARY_PATH`-style lists.

**Defaults applied when fields are zero/empty:** `backend_port_start=8201`, `backend_port_end=8299`, `max_models=2`, `idle_timeout_minutes=60`, `health_timeout_seconds=180`, `sweep_interval_seconds=15`, `nvidia_smi="nvidia-smi"`.

## Commands

```
llama-switch serve [config]    # start proxy (default if no command given)
llama-switch profile [config]  # load each model once, measure VRAM, save to vram-cache.json
llama-switch models [config]   # list configured models + VRAM estimates
llama-switch status [config]   # query a running server's /v1/loaded  (note: wired in usage but dispatched via `serve` path; see main.go)
llama-switch help
```

Config path resolution: positional arg → `$LLAMA_SWITCH_CONFIG` → `./config.yaml`.

## Build & run

```bash
go build                      # produces ./llama-switch
go vet ./...                  # static checks
go run . serve                # run without explicit build
./llama-switch serve          # run the built binary
./llama-switch profile        # (re)profile VRAM for all models
./llama-switch models         # print model table
```

systemd (user service, as deployed):

```bash
systemctl --user daemon-reload
systemctl --user restart llama-switch
journalctl --user -u llama-switch -f
```

## Conventions

- **Single package, flat layout.** All code is `package main` in the repo root. Do not introduce subpackages without reason; the project's style is one file per concern.
- **Go 1.26** toolchain; only stdlib + `yaml.v3`. Avoid adding dependencies for things the stdlib can do.
- **Comment style:** section dividers use `// ── Name ──────` banners; exported functions carry doc comments; `Caller must hold <mu>` notes annotate lock contracts — respect these when editing.
- **Locking:** `BackendManager.mu` guards the `backends` map and port allocator interactions during lifecycle. Methods suffixed `Locked` require the caller to hold the lock (e.g. `startBackendLocked`, `stopLocked`, `ensureCapacityLocked`, `evictLRULocked`). Preserve the double-checked-locking pattern in `EnsureLoaded` (RLock fast path → Lock slow path).
- **Process output** from each backend is piped through `logPipe` and printed to stdout with a `[model-id]` prefix — keep this; it's how service logs are read.
- **Streaming detection** in `forward()` is a naive string scan for `"stream":true` / `"stream": true`. If you touch request handling, be aware this is intentionally simple and does not fully parse JSON.
- **Errors during backend start** (port alloc fail, binary missing, health timeout) must release the port and clean up — mirror the cleanup in `startBackendLocked`.
- **Config is authoritative.** `config.yaml` reflects a real deployment with specific GGUF paths, GPU topology, and a bind address. Treat it as live configuration, not an example template.
- **No tests currently exist.** If adding code, consider that there is no existing test harness to extend — `go test ./...` will pass trivially today.

## Common tasks for agents

- **Add a new model:** add an entry under `models:` in `config.yaml`; run `llama-switch profile` so admission control has VRAM estimates.
- **Add a new HTTP endpoint:** add a path branch in `ProxyServer.handle` (`proxy.go`) and a `handleXxx` method. Management/model-list endpoints return JSON; proxying goes through `handleProxy` → `forward`.
- **Change eviction policy:** `ensureCapacityLocked` and `evictLRULocked` in `backend.go`. LRU is based on `Backend.lastReq`, updated via `Touch()` on every proxied request.
- **Change CLI surface:** `main.go` `switch cmd` block + `usageText`. `parseConfigArg` handles config-path discovery.
- **Rebuild after edits:** `go build` and replace the `llama-switch` binary; restart the systemd unit if running as a service.

## Things to watch out for

- `ReadTimeout`/`WriteTimeout` on the proxy server are intentionally `0` to allow long-lived streaming requests — do not set finite timeouts without accounting for streaming.
- The VRAM headroom constant is `1024` MB (1 GB) in `ensureCapacityLocked` (`backend.go`). It is applied **only to the primary GPU** (CUDA0 / index 0) to account for OS/display-server overhead; secondary GPUs have no display output and thus no headroom. Adjust there if changing the safety margin.
- Per-GPU admission control (`vramFitsPerGPU` in `vram.go`) checks each GPU the model targets individually. If per-GPU profile data is unavailable (legacy cache entries without `gpu_vram`), it falls back to the aggregate VRAM check. Keep this fallback intact.
- VRAM measurements are validated during profiling (`validateVRAMMeasurement` in `vram.go`): rejected if below 256 MB or below the model's `.gguf` file size. Both the `profile` command and auto-profiling during `serve` enforce this. Rejected measurements are not cached.
- `waitHealth` polls every 2 s; large models may take close to `health_timeout_seconds` (default 180 s) to become ready. The proxy returns `503` with `Retry-After: 30` if a model fails to load.
- `nvidia-smi` is required for VRAM-aware scheduling; if it is unavailable, `ensureCapacityLocked` falls through optimistically (loads without VRAM checks) — keep this fallback intact.
- `config.yaml` contains absolute paths specific to the deployment machine; don't generalize them.
